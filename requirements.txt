# Core: vectorize raster/PDF → DXF (no AutoCAD required)
opencv-python-headless>=4.8.0,<5.0.0
numpy>=1.24.0,<3.0.0
ezdxf>=1.1.0,<2.0.0
pymupdf>=1.23.0,<2.0.0
pillow>=10.0.0,<12.0.0
scikit-image>=0.22.0,<0.25.0
networkx>=3.2,<4.0
shapely>=2.0.0,<3.0.0

# Optional per-part split via OCR (install: pip install easyocr , or pip install -e ".[ocr]")
# easyocr>=1.7.0

# Dev / tests
pytest>=7.4.0,<9.0.0
